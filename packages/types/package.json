{
  "name": "@medsys/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm install typescript@^5.8.2 --save-dev && npx tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
   "typecheck": "npm install typescript@^5.8.2 --save-dev && npx tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured\""
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "@types/node": "^22.14.1"
  }
}
